name: Pipeline

on:
  push:
    paths:
      - 'trigger.txt'

jobs:
  vm:
    uses: ./.github/workflows/vm.yml
    secrets: inherit

  storage:
    needs: vm
    uses: ./.github/workflows/storage.yml
    secrets: inherit